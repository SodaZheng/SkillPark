interface:
  display_name: "SkillPark"
  short_description: "Search a bounded parked-skill candidate set for every request"
  default_prompt: "Use $skillpark to search parked skills with concise bilingual capability keywords and load only true trigger matches."
policy:
  allow_implicit_invocation: true
