interface:
  display_name: "SkillPark"
  short_description: "Route every request to a small parked-skill candidate set"
  default_prompt: "Use $skillpark to consume the local router result for this request and load only true matches."
policy:
  allow_implicit_invocation: true
